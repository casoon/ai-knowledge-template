# Deklariert die Kategorien der Wissensbasis. Neue Kategorie = neuer Eintrag
# hier, kein Code-Change in knowledge-lint nötig.
#
# kind: knowledge_entry — normaler Markdown-Eintrag mit Frontmatter, wird
#   gegen type/review_interval_days geprüft.
# kind: sops_secret — SOPS-verschlüsselte YAML-Datei, kein Frontmatter-Schema,
#   wird nur auf ein vorhandenes `sops:`-Metadatenfeld geprüft (= verschlüsselt).
# kind: assets — reiner Ablageort für Dateien, die aus _attachments.yml
#   referenziert werden. Keine Inhaltsprüfung.
#
# status_values/active_status: Das Lint-Tool (github.com/casoon/knowledge-lint)
# ist sprachunabhängig, Default ist Englisch (current/deprecated) — hier
# explizit auf unser deutsches Vokabular festgelegt.
#
# Quer zu den Kategorien liegt `sphere: geschaeftlich | privat` im Frontmatter.
# Kategorien, in denen beides vorkommt, bekommen zusätzlich die Unterordner
# `geschaeftlich/` und `privat/` — hier `konzepte/` und `dokumentation/`. Alle
# anderen sind ihrer Natur nach geschäftlich und bleiben flach. Siehe
# GOVERNANCE.md.

status_values: [aktuell, veraltet]
active_status: aktuell

categories:
  entscheidungen:
    kind: knowledge_entry
    type: entscheidung
    review_interval_days: 365
  prozesse:
    kind: knowledge_entry
    type: prozess
    review_interval_days: 365
  produkte:
    kind: knowledge_entry
    type: produkt
    review_interval_days: 180
  glossar:
    kind: knowledge_entry
    type: glossar
    review_interval_days: 365
  quellen:
    kind: knowledge_entry
    type: quelle
    review_interval_days: 365
  kunden:
    kind: knowledge_entry
    type: kunde
    review_interval_days: 90
  projekte:
    kind: knowledge_entry
    type: projekt
    review_interval_days: 30
  angebote:
    kind: knowledge_entry
    type: angebot
    review_interval_days: 30
  konzepte:
    kind: knowledge_entry
    type: konzept
    review_interval_days: 180
  dokumentation:
    kind: knowledge_entry
    type: dokumentation
    review_interval_days: 180
  auftragsbestaetigungen:
    kind: knowledge_entry
    type: auftragsbestaetigung
    review_interval_days: 365
  protokolle:
    kind: knowledge_entry
    type: protokoll
    review_interval_days: 90
  aenderungen:
    kind: knowledge_entry
    type: aenderung
    review_interval_days: 90
  abnahmen:
    kind: knowledge_entry
    type: abnahme
    review_interval_days: 365
  abrechnung:
    kind: knowledge_entry
    type: abrechnung
    review_interval_days: 365
  service:
    kind: knowledge_entry
    type: service
    review_interval_days: 90
  secrets:
    kind: sops_secret
  example-assets:
    kind: assets
